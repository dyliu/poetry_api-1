parameters:
  - name: imageName
    in: query
    description: 镜像名称
    required: true
    schema:
      type: string
  - name: security
    in: query
    description: 镜像id
    required: false
    schema:
      type: string
get:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - DeliveryCenter
  summary: 获取镜像安全扫描详情信息
  responses:
    '200':
      description: 镜像安全扫描参数
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/DeliverySecurity'