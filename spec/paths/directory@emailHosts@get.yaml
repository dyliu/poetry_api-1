parameters:
  - name: orgId
    in: query
    description: 组织ID
    required: true
    schema:
      type: integer
get:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - Directory
  summary: 获取邮件主机配置
  responses:
    '200':
      description: 邮件服务配置
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/EmailHost'