parameters:
  - name: orgId
    in: query
    description: 组织ID
    required: true
    schema:
      type: integer
put:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - Directory
  summary: 邮件主机配置更新
  requestBody:
    description: 邮件主机参数
    content:
      application/json:
        schema:
          $ref: '#/components/schemas/EmailHost'
  responses:
    '200':
      description: 更新成功
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/EmailHost'
